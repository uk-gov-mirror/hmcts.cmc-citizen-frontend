{% extends 'layout.njk' %}

{% set heading = 'Error' %}

{% block content %}
  <h1 class="govuk-heading-l">{{ error.message }}</h1>
  {% if error.status %}
    <h3 class="govuk-heading-m">{{ error.status }}</h3>
  {% endif %}
  <pre class="code">{{ error.stack }}</pre>
{% endblock %}
