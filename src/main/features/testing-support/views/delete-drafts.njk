{% extends "layout.njk" %}
{% from "form.njk" import linkButton, csrfProtection, errorSummary %}
{% from "date.njk" import dateInput %}

{% set heading = 'Delete drafts' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <form method="post" novalidate>
        {{ csrfProtection(csrf) }}

        <div class="notice">
          {{ linkButton('Delete claim draft', 'action[claim]', 'button button-secondary') }}
        </div>
        <div class="notice">
          {{ linkButton('Delete response drafts', 'action[response]', 'button button-secondary') }}
        </div>
        <div class="notice">
          {{ linkButton('Delete ccj drafts', 'action[ccj]', 'button button-secondary') }}
        </div>
      </form>
    </div>
  </div>
{% endblock %}
