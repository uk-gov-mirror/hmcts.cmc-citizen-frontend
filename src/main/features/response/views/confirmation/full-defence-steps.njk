{% set claimantName = claim.claimData.claimant.name %}

<p class="govuk-body">{{ t('If {{ claimantName }} accepts your defence the claim will be cancelled.', { claimantName: claimantName }) | safe }}</p>
<p class="govuk-body">{{ t('If they reject your defence the case will be reviewed and might go to a hearing.') }}</p>

{% if featureToggles.offer %}
  <h2 class="govuk-heading-m">{{ t('Settle out of court') }}</h2>
  <p class="govuk-body">{{ t('You may still <a class="govuk-link" href="{{ link }}">settle the claim out of court</a>. For example you could offer to fix goods you sold the claimant or suggest a payment.',
      { link: OfferPaths.settleOutOfCourtPage.evaluateUri({ externalId: claim.externalId }) }) | safe }}</p>
  <p class="govuk-body">{{ t('You can avoid getting a County Court Judgment if the claimant accepts your offer.') }}</p>
{% endif %}

{% if claim.response.freeMediation === YesNoOption.YES.option %}
  <h2 class="govuk-heading-m">{{ t('Mediation') }}</h2>
  <p class="govuk-body">{{ t('You have chosen to use the free mediation service to resolve the dispute with {{ claimantName }}. We’ll ask them if they want to mediate.', { claimantName: claimantName  }) }}</p>
  <p class="govuk-body">{{ t('If they agree to mediation we’ll email you to arrange an appointment.') }}</p>
  <p class="govuk-body">{{ t('If you can reach an agreement with {{ claimantName }} you won’t need to go to a hearing.', { claimantName: claimantName  }) }}</p>
{% endif %}
