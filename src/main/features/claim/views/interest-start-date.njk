{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, textArea, saveAndContinueButton, errorSummary %}
{% from "date.njk" import dateInput %}

{% set heading = 'Enter the date you want to claim interest from' %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <form novalidate method="post">
        {{ csrfProtection(csrf) }}

        <div class="govuk-form-group">
          {{ dateInput('date', form, hint = t('For example, {{ pastDate }}', { pastDate: pastDate | inputDate })) }}
          {{ textArea('Explain why you’re claiming from this date', 'reason', form, inputClass = 'full-width') }}
        </div>

        <div class="govuk-form-group">
          {{ saveAndContinueButton() }}
        </div>
      </form>
    </div>
  </div>
{% endblock %}
