{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, input, saveAndContinueButton, errorSummary %}

{% set heading = 'Enter a contact number (optional)' %}

{% block main_content %}
  <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">

        <p class="govuk-body">{{ t('We might need to contact you about your claim. We’ll only call on weekdays between 9am and 5pm.') }}</p>
        <form method="post">
          {{ csrfProtection(csrf) }}

          {{ input('Enter a contact number (optional)', 'number', form, '', labelClass = 'govuk-visually-hidden') }}
          {{ saveAndContinueButton() }}
        </form>
      </div>
  </div>
{% endblock %}
